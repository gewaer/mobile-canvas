import { StyleSheet } from "react-native";
import { colors } from "../../config/styles";

export default StyleSheet.create({
  paginationContainer: {
    paddingTop: 24,
    paddingBottom: 0
  },
  paginationDot: {
    width: 6,
    height: 6,
    borderRadius: 3
  },
  dotContainer: {
    marginHorizontal: 6
  }
});
